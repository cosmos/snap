{
    "version": 2, 
    "builds": [{
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "buildCommand": "cp ./src/rpcs.json dist/ && yarn workspace ui build",
          "tsconfig": "tsconfig.json" 
        }
      }]
}